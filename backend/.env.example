# AI Financial Statement Generation System - Backend Environment Variables

# ==================== AI API KEYS ====================
# Google Gemini API key (required)
# Get from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# OpenRouter API key for Grok models (required)
# Get from: https://openrouter.ai/
OPENROUTER_API_KEY=your_openrouter_api_key_here

# ==================== SUPABASE CONFIGURATION ====================
# Supabase project URL (required)
# Get from: https://supabase.com/
SUPABASE_URL=https://your-project.supabase.co

# Supabase anonymous key (required)
# Used for public, non-authenticated requests
SUPABASE_ANON_KEY=your_supabase_anon_key

# Supabase service key (required)
# Used for privileged operations (keep secure!)
SUPABASE_SERVICE_KEY=your_supabase_service_key

# ==================== EMAIL CONFIGURATION ====================
# SMTP server for sending alerts (optional)
# Example: smtp.gmail.com, smtp.office365.com
SMTP_SERVER=your_smtp_server

# SMTP port (typically 587 for TLS)
SMTP_PORT=587

# SMTP username
SMTP_USERNAME=your_email@example.com

# SMTP password (use app-specific password for Gmail/Office365)
SMTP_PASSWORD=your_smtp_password

# Email address to receive alerts
ALERT_EMAIL=alerts@yourcompany.com

# ==================== PROCESSING CONFIGURATION ====================
# Maximum file size in MB (default: 50)
MAX_FILE_SIZE_MB=50

# Maximum PDF pages to process (default: 100)
MAX_PAGES_PER_PDF=100

# Number of parallel workers for processing (default: 4)
MAX_WORKERS=4

# PDF rendering DPI (default: 300)
PDF_DPI=300

# Cache TTL in hours (default: 1)
CACHE_TTL_HOURS=1

# ==================== LOGGING ====================
# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# ==================== OPTIONAL: DEBUGGING ====================
# Enable debug mode (not recommended for production)
# DEBUG=false

# Database path for SQLite cache
SQLITE_DB_PATH=fs_audit.db
